import React from 'react';
import Auth from '../utils/auth';

const Signup = () => {

    return (
        <div>
            <h1>Signup</h1>
            <p>This is the signup page</p>
        </div>
    );
};

export default Signup;